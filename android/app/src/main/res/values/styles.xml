<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws the whole window; the framework action bar stays off. -->
    <style name="Theme.Bagholder" parent="@android:style/Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
